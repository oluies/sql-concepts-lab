{
  "name": "sql-concepts-lab",
  "private": true,
  "version": "0.1.0",
  "description": "Interactive SQL concepts trainer running DuckDB-WASM entirely in the browser",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "^1.31.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
